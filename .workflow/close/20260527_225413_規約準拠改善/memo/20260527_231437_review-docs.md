---
document_id: "1540013C-A5F8-4BB0-8E6B-ED21D6245380"
---

# review-docs 証跡: 規約準拠改善（親 + サブ6件の統括レビュー）

**command**: review-docs（実装前ドキュメントレビュー）
**対象（親）**: `00_要求定義.md`, `90_issues.md`
**対象（サブ）**: A〜F の各 `00_要求定義.md` / `01_要件定義.md`（計 12 ファイル）
**レビュー実施**: 2026-05-27（JST）
**レビューサイクル**: 第1回（指摘→修正）→ 第2回（再確認、指摘0件で終了）

---

## 1. 対象の実在確認

親ワークフロー直下に `00_要求定義.md`・`90_issues.md` が存在。`90_issues/` 配下に A〜F の 6 ディレクトリ（`20260527_225518_A_...` 〜 `20260527_230140_F_...`）が実在し、各々に `00_要求定義.md`・`01_要件定義.md` を確認。全 14 ファイルを監査対象とした。

## 2. 監査観点（8 項目）の総括

| # | 観点 | 結果 |
|---|------|------|
| 1 | テンプレート準拠 | OK（親 00 に「要求定義の全体像」+ Mermaid mindmap、全サブ 00 も同様。01 は §1〜8 完備） |
| 2 | document_id / issue_id 必須・整合 | OK（全 14 ファイル + memo に UUID。重複なし。親 issue_id `9EE981E8-...`、6 サブの issue_id がすべて 90_issues.md の表と一致。既存値の上書きなし） |
| 3 | 受け入れ基準の検証可能性 | OK（全 issue の効果・成功基準が ○/× 判定可能。曖昧表現は排除済み） |
| 4 | BDD 形式 | サブ A で 1 件指摘（A-1）→修正。他は OK |
| 5 | spec 準拠（01/02/03/06） | OK（禁止命名 helpers/misc/common/utils の使用なし。むしろ B/C で明示的に回避を記載。境界/CQRS/単一責務/優先順位が各 issue に反映） |
| 6 | 問題の網羅性（T1/T2・S1/S2/S3・S4・L1〜L5・D1/D2・R1） | OK（下表のとおり過不足なく割当。重複・抜けなし。引用行を実ソースで検証し一致） |
| 7 | プレフィックス・配置 | OK（issue フォルダ名・memo 名すべて YYYYMMDD_HHMMSS_ 規約準拠） |
| 8 | 整合性（親/90/サブ） | 親 00 で 1 件指摘（P1）→修正。修正後は依存順が 90_issues.md と整合 |

## 3. 問題 → issue 割当（網羅性検証）

| 問題 | 内容 | 担当 issue | 実ソース検証 |
|------|------|-----------|--------------|
| T1 | テスト 0 件 | A | nextDailyRun L186 / relativeTimeShort L203 / relativeNext L213, monitor.sh should_notify L20 |
| T2 | ビルド構成なし | A | swiftc コメントのみ・Package.swift 不在 |
| S1 | God class 662 行・境界なし | B | AppDelegate L28、662 行（wc 一致） |
| S2 | CQRS 未適用（toggleJob に command+query） | B | toggleJob L455 / launchctl list L476 |
| S3 | rebuildMenu 巨大メソッド | B | rebuildMenu L246 |
| S4 | メトリクス取得 3 箇所重複 | C | gatherMetrics L113-150 / mac-health L50-77 / monitor.sh L42-65 |
| L1 | mtime+14 のみで無限肥大化 | D | log.sh L26-27（`-mtime +14 -delete 2>/dev/null`） |
| L2 | 呼び出し偏在 | D | rotate_logs 呼出が一部ジョブのみ |
| L3 | .out/.err 対象外 | D | launchd plist の Standard{Out,Error}Path |
| L4 | ロックなし | D | cooldown read-modify-write / events 並行追記 |
| L5 | エラー握り潰し | D | log.sh `2>/dev/null` |
| D1 | docs 空 | E | docs/ が空（実確認） |
| D2 | ワークフロー成果物未整備 | E | — |
| R1 | 文字列補間シェル構築 | F | shell L634 / notify L627 / launchctl list 補間 L156,L476 |

→ 13 問題すべてが A〜F のいずれかに重複なく割り当てられている。issue 間の責務分界（B/F の ShellRunner 連携、C/F のセキュリティ分界、D の独立性、E の随時更新）も明記され矛盾なし。

## 4. 指摘一覧と対応

### P1（優先度: 低, 観点8 整合性）— 対応済み（修正）

- **場所**: 親 `00_要求定義.md` §7.2 スケジュールリスク 対策
- **内容**: 依存順を「A → B/C/F → D/E」と記載していたが、`90_issues.md` §推奨依存順は「D は比較的独立（A 後に着手可）」「E は随時更新」とし、Mermaid も A→D を直接結線。親 00 の記述だと D/E が B/C/F の後段に強制されるように読め、90_issues.md と不整合。
- **対応**: 親 00 §7.2 を「まず A、続いて B/C/F、D は A 後に比較的独立して着手可、E は随時更新」と書き換え、90_issues.md §推奨依存順への参照リンクを付与して整合。

### A-1（優先度: 中, 観点4 BDD 網羅）— 対応済み（サブ A memo に詳細）

- サブ A `01_要件定義.md` §2.2 ユースケース4 のメトリクス閾値判定 BDD シナリオが swap(超過)/pressure(critical) の 2 件のみで、受け入れ基準が要求する compressed/load および「未満→非通知」が欠落。→ シナリオを 6 件に拡充し実ソースの閾値変数名（THRESHOLD_COMPRESSED_GB、`ncpu × THRESHOLD_LOAD_AVG_MULTIPLIER`）に整合させて修正。

### サブ B/C/D/E/F

- 初回から指摘 0 件（各サブ memo 参照）。

## 5. 第2回（再確認）

P1・A-1 の修正後に親 00・サブ A 01 を再読。テンプレート必須セクション・document_id 不変・依存順整合・BDD と受け入れ基準の対応を再確認。**全 issue で残指摘 0 件**。

## 6. 修正ファイル

- `00_要求定義.md`（親 §7.2 依存順の整合）
- `90_issues/20260527_225518_A_テストビルド基盤確立/01_要件定義.md`（閾値判定シナリオ拡充）

## 7. 完了判定（review-docs DoD）

- 全対象 00/01 の既知指摘 0 件（達成）
- 各 issue（親 + サブ6）の memo/ に YYYYMMDD_HHMMSS_ プレフィックス memo を 1 件以上作成（達成。プレフィックスは TZ=Asia/Tokyo date 実行で取得）
- 04_review.md は作成していない（実装前ドキュメントレビュー専用）
- 書記（write-workflow-log）への記録は本サイクル完了後に実施
