---
document_id: "b4d7c2e1-3a5f-4c89-9e02-1f3a5b7c9d11"
---

# 実装前ドキュメントレビュー: make check の GitHub Actions 化

**対象**: `.workflow/20260528_135936_make_check_CI導入/`
**対象ファイル**: `00_要求定義.md`, `01_要件定義.md`
**レビュー日時**: 2026-05-28 14:02:56（JST）
**レビュー実施者**: requirement-discovery サブエージェント（自己レビュー）

> **位置付け**: 実装前ドキュメントレビュー（PHASES §レビュー成果物の配置ルール）。本ドキュメントは memo であり、04_review に相当する正式なレビュー成果物ではない。

---

## 1. レビュー観点

要求発見フェーズ（requirement-discovery）の DoD と、`.workflow/templates/00_要求定義.md` / `01_要件定義.md` のテンプレート必須セクションを満たしているかを確認する。

### 1.1 テンプレート充足

| 確認項目                                                          | 00 | 01 |
| ----------------------------------------------------------------- | -- | -- |
| frontmatter `document_id` が UUID 形式で記載                      | OK | OK |
| 00 frontmatter `issue_id` が UUID 形式で記載                      | OK | -  |
| 「要求定義の全体像」セクションが存在                              | OK | -  |
| 「要求定義の全体像」内に Mermaid マインドマップが存在             | OK | -  |
| §1.1 目的が 1 文・判定可能な記載                                  | OK | -  |
| §1.3 期待される効果が 1 件以上・○/× 判定可能な記載               | OK | -  |
| §6 成功基準が 1 件以上・○/× 判定可能な記載                       | OK | -  |
| §1〜§9 の全セクション存在                                         | OK | -  |
| §1〜§8 の全セクション存在（01）                                   | -  | OK |
| ユーザーストーリー（As a / I want to / So that）                  | -  | OK（4 件） |
| BDD シナリオ Given/When/Then                                      | -  | OK（6 シナリオ） |
| 受け入れテスト AT 表                                              | -  | OK（AT1〜AT6） |
| 参照元（親 00 / 先行 issue 等）の明記                             | OK | OK |

### 1.2 委譲パケットの確定済み前提との整合

| 確定済み前提（委譲パケット）                                                    | 00 / 01 への反映                                                          |
| ------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| runner: `macos-latest`                                                          | 00 §4.1, 01 §1.1 / §2.2 シナリオ 5・6 で明記                              |
| トリガー: pull_request（全ブランチ）+ push（main のみ）                         | 00 §2.2, 01 §2.1 ストーリー 1・2, §2.2 シナリオ 1・2                       |
| 任意ツール（shfmt/swift-format/swiftlint）は CI で導入しない・SKIP のままで完走 | 00 §5, 01 §2.1 ストーリー 3, §1.1                                          |
| shellcheck フォールバック（`brew install shellcheck`）                          | 00 §2.2 step 2 / §4.1, 01 §2.1 ストーリー 4, §2.2 シナリオ 5・6            |
| 先行 issue との独立した整備                                                     | 00 §1.2 / §8 で先行 issue 参照を明示                                       |

### 1.3 scope（やる/やらない）の明確化

| カテゴリ | 内容                                                                                                | 反映先         |
| -------- | --------------------------------------------------------------------------------------------------- | -------------- |
| やる     | `.github/workflows/check.yml` 作成、PR/main push トリガー、macos-latest 上の brew install shellcheck（必要時のみ）、make check 実行、失敗時 CI 赤 | 00 §2.2 / §6 / 01 §1.1 |
| やらない | 任意ツールの CI 導入、複数 runner マトリクス、キャッシュ最適化、README ステータスバッジ、PR ラベル自動化 | 00 §5          |

### 1.4 evidence_source の付与（CONCEPTS §外部根拠の必須化）

| 重要判断                          | evidence_source                                | 記載箇所       |
| --------------------------------- | ---------------------------------------------- | -------------- |
| runner: macos-latest              | human_decision（ユーザー回答 1）               | 00 §4.1        |
| トリガー範囲                      | human_decision（ユーザー回答 2）+ external_spec | 00 §4.1        |
| 任意ツール CI 導入なし            | human_decision（ユーザー回答 3）               | 00 §5          |
| shellcheck フォールバック         | human_decision（ユーザー回答 4）               | 00 §4.1        |
| `make check` 実行時間想定         | test_output（先行 issue 04_review §3.1）        | 01 §3.1        |

---

## 2. 指摘事項

### 2.1 軽微（記録のみ・次フェーズで対応可）

- **指摘 N-1**: 00 §1.1 目的「PR と main push のたびに `make check` を GitHub Actions で自動実行し、品質基盤を常時検証する。」 は半角・全角混合で 51 文字程度。テンプレート注記は「1 文・50 字以内」のため目安をわずかに超過している。判定可能性（1 文・主語・動詞・対象が明確）は満たしており致命的ではないが、次フェーズ（design-feature 等）で短縮余地がないか再検討する余地あり。
  - 対応方針: **本フェーズでは現状維持**（情報密度を優先）。後段で見直す余地があれば短縮する。
- **指摘 N-2**: AT4（make check 赤のとき CI 赤）は v1 では「自動 reproduction PR」を常設しない。実装フェーズで実証する手順は memo に書いたが、03_実装計画 で `__force_fail.sh` 注入の手順を明示しておくと verify-and-close で漏れにくい。
  - 対応方針: **次フェーズ（design-feature / 03_実装計画）で考慮**。本要求発見フェーズの DoD には影響しないため、本 memo に記録するのみ。

### 2.2 重大指摘

- **なし**。

---

## 3. レビュー判定

- **判定**: **指摘なし（軽微記録のみ）**で要求発見フェーズの DoD を満たす。
- **根拠**:
  - PHASES.md「要求」「要件」フェーズの DoD（目的・受け入れ基準・参照元 / ユーザーストーリー・受け入れ基準・BDD シナリオ）をいずれも満たしている。
  - テンプレートの必須セクションをすべて充足している。
  - 委譲パケットの確定済み前提（runner / トリガー / 任意ツール / shellcheck / 先行 issue との関係）が 00/01 のいずれかに明示されている。
  - 軽微指摘 2 件はいずれも本フェーズの完了判定に影響しない。

## 4. 完了判定（DoD）

- [x] 00_要求定義.md がテンプレート必須セクションを満たして存在
- [x] 01_要件定義.md がテンプレート必須セクションを満たして存在
- [x] BDD シナリオ ID（シナリオ 1〜6）と AT（AT1〜AT6）の対応が取れている
- [x] 参照元（先行 issue・親ドキュメント等）が 00/01 に記載されている
- [x] 実装前ドキュメントレビュー memo（本ファイル）が存在し、判定が記載されている
- [x] 次フェーズ（design-feature）への引き継ぎ事項（軽微指摘 2 件）が記録されている

## 5. 次フェーズへの引き継ぎ事項

1. 02_設計 では `.github/workflows/check.yml` の具体 YAML 構造（jobs / steps / actions/checkout バージョン / shellcheck フォールバック step）を決定する。
2. 03_実装計画 では AT4 の実証手順（`__force_fail.sh` 注入による CI 赤確認）と元状態への復元手順を明示しておく。
3. レビューフェーズ（verify-and-close）では、実 PR 発火後の Actions 実行ログを `test_output` として 04_review §3 に貼り付ける。
