---
document_id: "ea870a9f-3edc-4f4e-96b3-ce9eca44b6e9"
---

# 実装前ドキュメントレビュー: 02_設計.md / 03_実装計画.md

**対象**: `.workflow/20260528_135936_make_check_CI導入/`
**対象ファイル**: `02_設計.md`, `03_実装計画.md`
**レビュー日時**: 2026-05-28 14:14:59（JST）
**レビュー実施者**: design-feature サブエージェント（自己レビュー）

> **位置付け**: 実装前ドキュメントレビュー（PHASES §レビュー成果物の配置ルール）。本ドキュメントは memo であり、04_review に相当する正式なレビュー成果物ではない。

---

## 1. レビュー観点

design-feature command の DoD（責務・参照関係・テスト観点が 02 に記載、タスク分解・テスト仕様（BDD）が 03 に記載、01 BDD と 03 テスト観点が対応）と、`.workflow/templates/02_設計.md` / `03_実装計画.md` のテンプレート必須セクションを満たしているかを確認する。委譲パケットの確定済み設計事項（YAML 構造案・AT4 実証手順・GitHub Actions best practices）が漏れなく反映されているかも併せて確認する。

### 1.1 テンプレート充足

| 確認項目（02_設計） | 結果 |
| --- | --- |
| frontmatter `document_id` が UUID 形式で記載 | OK（`68089c4f-...`） |
| §1 設計概要（設計方針・設計原則） | OK（UNIX 哲学・単一責務・明確な境界・AI フレンドリー設計） |
| §2 アーキテクチャ設計（責務・境界・参照関係・システムアーキテクチャ・コンポーネント構成・データフロー） | OK |
| §2.1.1 責務一覧表 | OK（5 コンポーネント） |
| §2.1.2 境界 | OK（入力・出力・他責務との境界） |
| §2.1.3 参照関係 | OK（単方向・循環なし） |
| §2.2 採用パターン・根拠 | OK（Thin Wrapper + Pipeline 再利用、spec/06 優先順位 1〜3 根拠） |
| §2.2.1 アーキテクチャ図（Mermaid） | OK |
| §2.4 データフロー（Mermaid sequence） | OK |
| §3 機能設計（1〜4 機能、処理フロー Mermaid） | OK |
| §4 データ設計（YAML 構造） | OK（4.1 に確定 YAML 全体像） |
| §5 API 設計（インターフェース一覧・詳細） | OK |
| §6 テスト戦略（6.1 割当表・6.2 監査証跡） | OK |
| §7 UI 設計 / §8 セキュリティ設計 / §9 非機能要件 | OK |
| §10 evidence_source 一覧 | OK（10 件） |
| §11〜§13 参考・前後ステップ | OK |

| 確認項目（03_実装計画） | 結果 |
| --- | --- |
| frontmatter `document_id` が UUID 形式で記載 | OK（`96008ef4-...`） |
| §1.1 実装方針（1〜2 文・空欄不可） | OK |
| §1.2 実装順序（Gantt 含む） | OK |
| §2 タスク分解（T1〜T7、各 §2.x.1〜§2.x.6） | OK |
| 各タスクの §2.x.3 テスト観点（単体・結合 API・E2E・バリデーション） | OK |
| 各タスクの §2.x.4 単体テスト仕様（BDD + テストコード例＋GWT インラインコメント） | OK |
| §テスト観点（全体要約） | OK |
| §単体テスト（全体方針） | OK |
| §BDD（01 BDD との対応表） | OK |
| §3 実装スケジュール / §4 テスト計画 / §5 リスク管理 | OK |
| §6〜§8 参考・前後ステップ | OK |

### 1.2 委譲パケットの確定済み前提との整合

| 確定済み前提 | 02 / 03 への反映 |
| --- | --- |
| `on: pull_request:`（branches なし）+ `push: branches: [main]` | 02 §3.1 / §4.1 / §5.2、03 T1 §2.1.2 |
| `runs-on: macos-latest` | 02 §2.3 / §3.4 / §4.1、03 T1 §2.1.2 |
| 3 step（checkout / shellcheck フォールバック / make check） | 02 §2.3 / §3.2 / §3.3 / §4.1、03 T1 §2.1.2 |
| `actions/checkout@v4`（メジャー pin） | 02 §5.2 I3 / §10、03 T1 §2.1.2 |
| `permissions: contents: read` | 02 §3.4.2 / §8.1 / §10、03 T1 §2.1.2 |
| `timeout-minutes: 30` | 02 §3.4.2 / §10、03 T1 §2.1.2 |
| `concurrency: cancel-in-progress: true` | 02 §3.4.2 / §10、03 T1 §2.1.2 |
| shellcheck フォールバック（command -v + brew install） | 02 §3.2 / §5.2 I4、03 T4（ローカル分岐テスト）+ T6（AT5/AT6 実 PR） |
| キャッシュ最適化を v1 で除外 | 02 §3.4.3 / §2.1.2、03 §1.1（言及） |
| AT4 実証手順（`__force_fail.sh` 注入＋復元、先行 issue 04_review §4 AT6 踏襲） | 03 T5 §2.5.2 / §2.5.4 |
| GitHub Actions の version pin / 最小権限 / timeout / concurrency 全反映 | 02 §3.4 / §10 で網羅 |
| README ステータスバッジは v1 除外（00 §5）→ 言及程度の判断のみ | 03 T7 §2.7.1 / §2.7.2 |

### 1.3 01 BDD（シナリオ 1〜6 / AT1〜AT6）と 03 テスト観点の対応

03 §BDD の対応表で 01 BDD シナリオ 1〜6 / AT1〜AT6 すべてが T1〜T6 のいずれかのタスク BDD と紐付いている。

### 1.4 evidence_source の付与

| 重要判断 | evidence_source | 記載箇所 |
| --- | --- | --- |
| runs-on: macos-latest | human_decision（00 §4.1） | 02 §10 |
| トリガー範囲 | human_decision（00 §4.1）+ external_doc | 02 §10 |
| actions/checkout@v4 採用 | external_doc | 02 §10 |
| permissions: contents: read | external_doc | 02 §10 |
| concurrency / timeout | external_doc | 02 §10 |
| AT4 実証手順 | existing_code + test_output（先行 issue 04_review §4 AT6） | 02 §10 / 03 T5 §2.5.1 |
| 任意ツール CI 導入なし | human_decision（00 §5） | 02 §10 |
| shellcheck フォールバック | human_decision（00 §4.1）+ external_doc | 02 §10 |
| `make check` 実行時間想定 | test_output（先行 issue 04_review §3.1） | 02 §9.1 |
| `actions/cache` 不採用 | human_decision（00 §5 / 01 §4） | 02 §10 |

---

## 2. 指摘事項

### 2.1 軽微（記録のみ・次フェーズで対応可）

- **指摘 N-3**: 02 §3.4.2 で `concurrency.group` を `check-${{ github.workflow }}-${{ github.ref }}` としたが、`github.workflow` を入れる必要性は薄い（本 workflow が単一ファイル運用のため）。実装フェーズで簡素化（`check-${{ github.ref }}`）するかを判断する余地がある。判定可能性・要件充足には影響しない。
  - 対応方針: **実装フェーズ（T1）で文字数・読み下しやすさを優先して再判断**。本フェーズの DoD には影響しないため記録のみ。
- **指摘 N-4（先行レビュー N-1 の再評価）**: 00 §1.1 目的「PR と main push のたびに `make check` を GitHub Actions で自動実行し、品質基盤を常時検証する。」 は依然として 50 字目安をわずかに超過する。再短縮例: 「PR と main push で `make check` を CI 実行し、品質基盤を常時検証する。」（情報量を維持）。
  - 対応方針: **本フェーズでは現状維持**（判定可能性は十分・短縮しても要件は不変）。実装フェーズで合わせて見直す余地があれば反映する。
- **指摘 N-5**: 03 T5 のテストコード例に sed の自動書き換え例を載せたが、実作業ではユーザーの Edit ツール手動編集の方が安全。コード例直後にその旨を注記済み（§2.5.4 末尾）だが、強調を上げる余地はある。
  - 対応方針: **本フェーズでは現状維持**。実装フェーズ T5 着手前に T5 §2.5.4 の注記を再読すれば事故を防げる程度の指摘。

### 2.2 重大指摘

- **なし**。

---

## 3. レビュー判定

- **判定**: **指摘なし（軽微記録のみ）**で design-feature コマンドの DoD を満たす。
- **根拠**:
  - 02_設計 / 03_実装計画 がテンプレート必須セクションを完全充足。
  - 委譲パケットの確定済み前提（YAML 構造・version pin・最小権限・timeout・concurrency・shellcheck フォールバック・AT4 実証手順・v1 除外項目）がすべて反映されている。
  - 01 BDD（シナリオ 1〜6 / AT1〜AT6）と 03 のタスク別 BDD が 1 対 1 で対応している（03 §BDD の対応表）。
  - 重要設計判断に `evidence_source` が記載されている（02 §10）。
  - サブ issue 分割の必要なし（90_issues.md 不要）。
  - 軽微指摘 N-3 / N-4 / N-5 はいずれも本フェーズの完了判定に影響しない。

---

## 4. 完了判定（DoD）

- [x] 02_設計.md がテンプレート必須セクションを満たして存在
- [x] 03_実装計画.md がテンプレート必須セクションを満たして存在
- [x] 02 に責務・境界・参照関係・テスト観点・API・evidence_source が記載されている
- [x] 03 にタスク分解（T1〜T7）・各タスクの BDD（§2.x.4）が記載されている
- [x] 01 BDD（シナリオ 1〜6 / AT1〜AT6）と 03 のテスト観点が対応している（§BDD 対応表）
- [x] サブ issue 分割なし → 90_issues.md 不要
- [x] 実装前ドキュメントレビュー memo（本ファイル）が存在し、判定が記載されている
- [x] 次フェーズ（implement-feature）への引き継ぎ事項（軽微指摘 N-3〜N-5）が記録されている

---

## 5. 次フェーズへの引き継ぎ事項

1. implement-feature では T1（`.github/workflows/check.yml` 新規作成）から開始する。02 §4.1 の確定 YAML 構造をそのまま採用する。
2. T1 着手時、軽微指摘 N-3（concurrency.group の簡素化可否）を判断し、簡素化する場合は 02 §3.4.2 / §4.1 を併せて更新する。
3. T5（AT4 ローカル実証）は **必ず復元手順まで完遂**する。Makefile の改変をコミットしない。
4. T6（実 PR 実証）の各 Actions ログを memo に保存し、verify-and-close で 04_review §3 / §4 へ移送する。
5. T7（README）はバッジを追加しない（00 §5）。CI 言及を 1〜2 行で追記するかは実環境での AT3 緑確認後に判断する。
