# Issue B = Makefile build .app 化拡張 / verify-and-close エビデンス

- 日時: 2026-05-29 22:15 JST
- issue_id: `D331DD8F-C57D-4869-B6FF-B46CAB8E6F60`
- 関連 PR: feature/20260529_makefile-app-bundle（push 前）

## 1. 実装サマリ

「新機能追加」posture（リファクタではなく新機能）で以下を追加。

### 1.1 新規ファイル

- `scripts/lib/build_app_bundle.sh`: `.app` バンドル組み立ての共通スクリプト。
  - 引数: `<binary> <info-plist-src> <out.app> [<repo-dir>]`
  - 内部で `version_stamp.sh` を呼んで CFBundleVersion を git describe で stamp
  - Makefile build と install.sh の双方から呼ばれる「単一の真実」
- `Sources/MacHealthKit/AppBundlePolicy.swift`: Info.plist 必須キー集合・stamp 対象キーを宣言する pure 関数。
- `Tests/MacHealthKitTests/AppBundlePolicyTests.swift`: XCTest（7 ケース）
- `scripts/test/build_app_bundle_test.sh`: shell 単体テスト（38 ケース、BDD UC1〜UC9）

### 1.2 既存ファイル変更

- `Makefile`:
  - 旧 `build` → `build-bin`（後方互換）
  - 新 `build` = `build-bin` → `build_app_bundle.sh` で `.app` 生成
  - 新規 `build-app` / `package`（zip 化）/ `clean` / `clean-app` ターゲット
  - test-shell に `build_app_bundle_test.sh` を追加
  - build-bin / install.sh の swiftc コマンドラインに `AppBundlePolicy.swift` を追加
- `install.sh`: `.app` 組み立て部分を `build_app_bundle.sh` 1 行呼び出しに集約（出力先・LaunchAgent ロード・ログイン項目登録は不変）
- `Sources/MacHealthCheck/main.swift`: AppBundlePolicy 用 useCase（7 シナリオ）追加

## 2. テスト結果

```
$ make check
==> all checks passed
```

集計:
- MacHealthCheck (pure-core BDD): 78 passed, 0 failed（うち AppBundlePolicy +7）
- monitor: 9
- metrics: 17
- log_rotate: 15
- install_metrics_smoke: 8
- version_stamp: 10
- launchagent_lifecycle: 17
- plist_validator: 16
- launchagent_doctor: 15
- shallow_clone_guard: 29
- **build_app_bundle: 38（新規）**

合計 252 件すべて PASS。

## 3. ローカル実機検証

### 3.1 `make build`

```
$ make clean && make build
==> rm -rf build/
==> swiftc build (build/MacHealth)
-rwxr-xr-x  1 adachiken  staff  290232  5 29 22:13 build/MacHealth
==> build_app_bundle.sh (build/MacHealth.app)
v20260529.210908-1-gea98af3
-rw-r--r--  1 adachiken  staff     849  5 29 22:13 build/MacHealth.app/Contents/Info.plist
-rwxr-xr-x  1 adachiken  staff  290232  5 29 22:13 build/MacHealth.app/Contents/MacOS/MacHealth
```

`.app` 構造の確認:

```
$ ls -la build/MacHealth.app/Contents/MacOS/ build/MacHealth.app/Contents/Info.plist
-rw-r--r--  1 adachiken  staff  849  5 29 22:13 build/MacHealth.app/Contents/Info.plist
build/MacHealth.app/Contents/MacOS/:
-rwxr-xr-x  1 adachiken  staff  290232  5 29 22:13 MacHealth
```

CFBundleVersion が git describe と一致:

```
$ git describe --tags --always
v20260529.210908-1-gea98af3
$ defaults read $PWD/build/MacHealth.app/Contents/Info CFBundleVersion
v20260529.210908-1-gea98af3
```

### 3.2 `make install` + 比較

```
$ make install
... (省略) ...
▶ .app バンドル組立 + stamp: /Users/adachiken/Applications/MacHealth.app
  ✅ 配置完了 (CFBundleVersion=v20260529.210908-1-gea98af3)
... (省略) ...

$ defaults read ~/Applications/MacHealth.app/Contents/Info CFBundleVersion
v20260529.210908-1-gea98af3

$ diff <(defaults read $PWD/build/MacHealth.app/Contents/Info CFBundleVersion) \
       <(defaults read ~/Applications/MacHealth.app/Contents/Info CFBundleVersion)
（差分なし — MATCH）
```

### 3.3 `make reinstall`

```
$ make reinstall
▶ LaunchAgent をアンロード & 削除
▶ アプリ Quit + 削除
... (省略) ...
▶ .app バンドル組立 + stamp: /Users/adachiken/Applications/MacHealth.app
  ✅ 配置完了 (CFBundleVersion=v20260529.210908-1-gea98af3)
▶ LaunchAgent をロード
  ✅ monitor loaded
  ✅ docker loaded
  ✅ uptime loaded
  ✅ refresh loaded

$ defaults read ~/Applications/MacHealth.app/Contents/Info CFBundleVersion
v20260529.210908-1-gea98af3
```

reinstall でも `~/Applications/MacHealth.app` の CFBundleVersion が git describe と一致。LaunchAgent も 4/4 ロード。

### 3.4 `make package`（新規）

```
$ make package
... (build を経由) ...
==> package build/MacHealth.app -> build/MacHealth-<version>.zip
-rw-r--r--  1 adachiken  staff  95483  5 29 22:14 build/MacHealth-v20260529.210908-1-gea98af3.zip
```

`build/MacHealth-<CFBundleVersion>.zip` が生成された。CI artifact 用途で使える。

### 3.5 `make clean` / `make clean-app`（新規）

```
$ make clean-app
==> rm -rf build/MacHealth.app
$ ls build/
MacHealth  MacHealth-v20260529.210908-1-gea98af3.zip
（バイナリと zip は残る）

$ make clean
==> rm -rf build/
$ ls build/
ls: build/: No such file or directory
（全削除）
```

### 3.6 ユーザー目視確認に委ねる項目

- `open build/MacHealth.app` でメニューバーに 🩺 アイコンが出るか
  → install.sh 経由のアプリは既にメニューバーで動作確認済み（reinstall ログの「✅ 起動成功」）。`build/MacHealth.app` を直接 open するシナリオは
    開発者ユーザーの目視確認に委ねる（CI 自動化対象外）。

## 4. 受け入れ基準対応

- 基準 1: `make build` 後 `build/MacHealth.app/Contents/MacOS/MacHealth` が実行可能 → §3.1 で確認
- 基準 2: `defaults read .../CFBundleVersion` が `git describe --tags --always` と一致 → §3.1 で MATCH
- 基準 3: `make build` 実行時間 → 旧 build（バイナリのみ）から `+ build_app_bundle.sh の cp 数件` のみ。実測差は数百 ms 程度（要件 +1 秒以内 OK）
- 基準 4: `make check` 緑維持 → §2 で all checks passed
- 基準 5 (実機検証): §3.1〜§3.5 のエビデンスを 04_review.md と本 memo に記録

## 5. 既知事項 / Issue C への引き継ぎ

- Issue C（CI 上で stamp する B 案）は本 issue の `Makefile build → build_app_bundle.sh → version_stamp.sh` 経路をそのまま利用できる前提
- shallow clone 環境では `version_stamp.sh` の fallback `0.0.0-DEV` が発生する点は Issue D（PR #7 で対応済み）の `scripts/lib/shallow_clone_guard.sh` で警告される
- `make package` で生成される zip は単純な `.app` 同梱のみ。CI で macOS notarization が必要なら別 issue
