# verify-and-close メモ: shallow clone ガード + README 注意書き追加

**日時**: 2026-05-29 21:36 JST
**issue_id**: `5D5DD113-535C-4326-82B2-74F228E2F3D5`
**parent (requirement-discovery)**: `F3018A92-4441-460C-8DDF-35BBE2195454`
**branch**: `feature/20260529_shallow-clone-guard`

---

## 1. 実施サマリ

- 当初の README 注意書きスコープに加え、**新規機能** `scripts/lib/shallow_clone_guard.sh` を追加。
- `install.sh` の swiftc ビルド直前に guard を呼び、shallow clone 環境を構造的に検出。
- 環境変数 `MACHEALTH_AUTO_UNSHALLOW=1` で `git fetch --tags --unshallow` を自動実行。
- BDD 単体テスト `scripts/test/shallow_clone_guard_test.sh`（29 件・全 PASS）を新規作成。
- README に shallow clone の影響説明と対応方法（3 通り）を callout で追記。
- `make check` 緑（lint-shell / cycles / security / 全シェルテスト + Swift pure-core）。
- 実機エビデンス（通常 clone / shallow clone + AUTO=1 / fallback 再現）を 04_review に添付。

---

## 2. 変更ファイル

| 種別 | パス | 内容 |
|---|---|---|
| 新規 | `scripts/lib/shallow_clone_guard.sh` | shallow 検出 + 警告 + auto unshallow |
| 新規 | `scripts/test/shallow_clone_guard_test.sh` | BDD 単体テスト 29 件 |
| 更新 | `install.sh` | §4.5 で guard を呼ぶ 1 ブロック |
| 更新 | `README.md` | install 手順節に注意書き callout |
| 更新 | `Makefile` | `test-shell` のフォールバックランナーに新規テストを追加 |
| 更新 | `.workflow/.../00_要求定義.md` | スコープ追補（新機能追加 posture） |
| 更新 | `.workflow/.../01_要件定義.md` | UC3-S1/S2/S3 + UC4-S1/S2 追加 |
| 更新 | `.workflow/.../02_設計.md` | §2.1 責務追加 + §3.2 新機能設計 + §6 テスト表更新 |
| 更新 | `.workflow/.../03_実装計画.md` | タスク 4/5/6 追加 |

---

## 3. テスト規約遵守

- `.agents/RULES.md` §テスト戦略必須要件
- `.agents/TEST_BDD_FORMAT.md` §0/§1/§2/§3
- 参考実装: `scripts/test/launchagent_lifecycle_test.sh`、`scripts/test/version_stamp_test.sh`

各テストケースに **ユースケース:**（ファイル冒頭）/ **シナリオ:**（各 setup ブロック直前）/ **Given/When/Then** インラインコメント（各ブロック直上）を必須記載。

---

## 4. workflow.db ログ

`.agents/scripts/write-workflow-log.sh` で本 verify-and-close を記録。

---

## 5. 次のステップ

- 04_review.md 作成（本メモと並行）
- close 移動
- commit 分割（実装 / テスト / docs / close）
- PR 作成（base=main, head=feature/20260529_shallow-clone-guard）
- CI 緑後 squash merge
