# memo: 判断根拠 / 残課題（ビルド時バージョン自動 stamp）

**作成日時 (JST)**: 2026-05-29 11:01:59

## 採用ブランチ判断

- 本 issue は**ドキュメント作成のみ**で実装を含まないため、本依頼の範囲ではブランチを切り替えず、現在の `feature/20260529`（PR #3 のブランチ）上で `.workflow/20260529_105524_ビルド時バージョン自動stamp/` を追加した。
- 理由：
  - 新ブランチを切ると PR #3 と本 issue 文書の追跡が分散する。
  - `main` から `feature/20260530_build-time-version-stamp` を切る選択は、ドキュメントだけが先に main に乗ると PR #3 の review 中ノイズになるため未採用。
  - PR #3 マージ後、ユーザー判断で issue ドキュメントを `main` または新 feature ブランチへ整理し直すのが最もコスト低と考える。
- なお commit / push は本依頼の範囲外として未実施。ユーザーがブランチを決め次第、改めて commit してもらう想定。

## ユーザー判断ポイント（再掲）

1. **tag 命名規約**:
   - A 案（既存日時タグ `vYYYYMMDD.HHMMSS` 流用） / B 案（semver タグ `v1.3.0` に切替）。
   - 推奨: A 案（追加 CI 変更ゼロ、既存 `create-release.yaml` と整合）。
2. **Info.plist テンプレ値**:
   - A 案 `0` / B 案 `0.0.0-DEV` / C 案 開発中も stamp する前提でテンプレ値は実質未使用。
   - 推奨: B 案（テンプレ値のまま配布された場合の異常検知が容易）。
3. **PR #3 とのマージ順序**:
   - 本 issue の実装着手は PR #3 マージ後を強く推奨（Info.plist の手動 `1.3` 値とのコンフリクトを避けるため）。
4. **既存 CI ワークフローの扱い**:
   - 既存 `create-release.yaml` を流用するか拡張するかの判断は 02_設計 §10 に集約。
5. **About 動的取得の fallback 文言**:
   - 推奨: `"バージョン 不明"`。クラッシュさせない方針を優先。

## 残課題

- 実装本体は本 issue では未実施（範囲外）。実装着手時に新 issue または本 issue ディレクトリ配下の execution フェーズで開始する想定。
- `04_review.md` は実装後に作成する（本 issue では空テンプレすら作成しないことで、誤って review 工程に進んだ印象を与えないようにした）。
- 既存正本ルール `.agents/spec/04_仕様更新ルール.md` の追記内容（T7）は、実装着手時に PR #3 マージ後の最新版を見て差分を取ること。
