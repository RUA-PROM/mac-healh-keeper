# review-docs cycle 1 — 実装前ドキュメントレビュー

- 取得プレフィックス: `20260529_111236`（`.agents/scripts/memo-prefix.sh` 実行値）
- 対象 issue: `.workflow/20260529_105524_ビルド時バージョン自動stamp/`
- 対象ドキュメント: `00_要求定義.md` / `01_要件定義.md` / `02_設計.md` / `03_実装計画.md`
- command: review-docs / cycle: 1
- mode: standard

## 1. レビュー観点

- `.agents/commands/review-docs.md` / `.agents/workflow/PHASES.md` の監査観点
- `.agents/RULES.md` のドキュメント規約
- `.agents/spec/04_仕様更新ルール.md` / `.agents/spec/05_契約変更ルール.md` / `.agents/spec/06_設計判断の優先順位.md`
- **実装との照合**: `install.sh` / `Makefile` / `Package.swift` / `src/Info.plist` / `src/MacHealth.swift` / `Sources/MacHealthKit/` / `Sources/MacHealthCheck/` / `Tests/MacHealthKitTests/` / `scripts/{bin,lib,test,lint}/` / `.github/workflows/{check.yml,create-release.yaml}` / `docs/` / 既存 close issue `20260529_083530_メトリクス非表示修正`

## 2. 指摘一覧（cycle 1）

| ID | 場所 | 内容 | 優先度 | 対応方針 |
| --- | --- | --- | --- | --- |
| P0-01 | 02_設計 §2.1.1, 2.1.3, 2.2.2, 2.4, 3.4 / 03_実装計画 §2.6 / 01_要件定義 §2.2 UC5 / 00_要求定義 §1.3 効果 3 | CI 実装の実態と乖離: 既存 `create-release.yaml` の `runs-on:` は **`ubuntu-latest`**（plutil 不可・`gh release create --generate-notes`）。設計は暗に macOS-latest を前提とし、stamp ロジックが CI で実行されるような表記を含む。実装は **install.sh / make build 経路（ローカル）のみで stamp** し、CI は tag/Release 発行に専念する設計を明示する必要がある。01 UC5 §5.1 制約「CI runner は macOS-latest を維持」も明確な誤り（既存 release は ubuntu-latest が現状）。 | P0 | ドキュメント側を実態に合わせて修正。02 §3.4 / §2.1.1 / §2.2.2 と 01 §5、00 §3.1.2 を「Release は ubuntu-latest（git/gh のみ）」と訂正。stamp は CI で実行しない旨を明記。 |
| P0-02 | 02_設計 §2.1.1 / 03 §2.6 | ワークフローのファイル拡張子表記: 設計で `.github/workflows/create-release.yaml` と書いているのは正しい（実体は `.yaml`）。問題は無し。だが 01 §4.1「create-release ワークフロー」の表記がファイル名指定として揺らぐ箇所がある。完全表記を統一する。 | P2 | 設計・要件・00 通しで `create-release.yaml`（拡張子付き）で揃える。 |
| P0-03 | 03_実装計画 §2.4 / 02 §2.1.1 (Makefile 行) | `make build` の実装と乖離: 実際の `Makefile` の `build:` ターゲットは `build/MacHealth` バイナリのみ生成し、**.app バンドルや Info.plist 配置を行わない**（設計上「.app バンドルや LaunchAgent 配置は install.sh が担う」とコメント明記）。03 §2.4 「`make build` 後の `.app/Contents/Info.plist` の `CFBundleVersion` が…」「`make build` 経路でも同値」は誤り。`make build` は `.app` を作らないため stamp 対象が無い。 | P0 | 03 §2.4 と 02 §2.1.1 / §2.2.2 / §2.4 / §10-4 を「`make build` は `build/MacHealth` バイナリのみ生成のため stamp 対象なし。stamp は install.sh 経路のみ」と修正。`make build` 経由で `Info.plist` も別途コピー＆stamp する設計案を採るか、`build` ターゲットを変更しないかは設計判断で「変更しない（install.sh のみ stamp 対象）」に倒す。02 §10-4 の「make build 経路にも stamp を組み込む（推奨）」を「make build は .app を作らないため stamp 対象外。経路差は構造的に発生しない」に修正。 |
| P0-04 | 03 §2.6.5 / §2.8.5 依存関係 | 03 のタスク依存関係 `T6 depends on T2..T5` は OK だが、T4 が「.app に stamp」を前提しているため、T4 を「CI 流用確認（A 案）に統合」または削除する必要がある。 | P0 | P0-03 と連動。T4 を「make build の stamp 整合確認（実装変更なし）」に再定義、もしくは T4 削除。タスク表・依存関係を更新。 |
| P1-05 | 03 §2.8.2 docs 更新項目 | 03 §2.8.2 で「`docs/04_機能設計/` 配下の関連節を更新（バージョン管理の責務分離の節を追加）」とあるが、新規追加先（例: `docs/04_機能設計/CI・Release自動化/README.md` か新規ディレクトリか）が未確定。実装の `docs/04_機能設計/CI・Release自動化/README.md` の中身（F010-S2 の処理フロー）に追記するか別 docs を立てるかを明示する。 | P1 | 03 §2.8.2 を「`docs/04_機能設計/CI・Release自動化/README.md` に §バージョン stamp の責務分離 を追記」または新規 `docs/04_機能設計/バージョン管理/README.md` 案を 02 §10 の追加判断ポイントとして明示。 |
| P1-06 | 02 §6.1 / 03 §2.2.3 §2.3.3 | テスト戦略表で `version_stamp.sh` の単体テストを「bats もしくは bash テストで実装」と書いている。既存実装の `scripts/test/` は `*_test.sh`（assert 自作）+ `*.bats`（bats 用）の **両方が混在**。`make test-shell` のフォールバックは `monitor_test.sh`, `metrics_test.sh`, `log_rotate_test.sh`, `install_metrics_smoke_test.sh` を直接呼ぶ形式で、bats は `bats scripts/test/` 経由のみ。03 §2.2 / §2.8 の新規 smoke を **`Makefile` の `test-shell` フォールバック配列に追加する step** を明示する必要がある（実装計画として漏れている）。 | P1 | 03 §2.2.3 / §2.8.2 に「Makefile `test-shell` ターゲットのフォールバック側スクリプト配列に新規 smoke を追加」を実装内容として追加。 |
| P1-07 | 02 §10-6 / 03 §2.6 | 「既存日時タグ路線維持で十分」と書いているが、実際の `create-release.yaml` には `fetch-depth: 0` が無く、`actions/checkout@v4` の浅 clone（depth=1）では `git describe --tags` が tag を見つけない可能性が高い。これは設計の「tag が打たれた commit から install すると tag 名が CFBundleVersion に入る」の前提に直接効く（00 §6 基準 3 / 01 UC2）。02 §10 のユーザー判断ポイントに「CI 側の fetch-depth: 0 設定」を追加する必要がある。 | P1 | 02 §10 に「CI checkout の fetch-depth: 0 設定（または既存日時タグの完全 fetch）」を追加判断ポイントとして明記。エンドユーザーが clone する想定の install.sh 経路では git clone のデフォルト挙動（全 tag 取得）が前提だが、これも 02 §10 に明示する。 |
| P1-08 | 03 §2.3.4 BDD コード / 03 §2.8.4 BDD コード | smoke BDD コードブロックで `defaults read "$HOME/Applications/MacHealth.app/Contents/Info" CFBundleVersion` を比較対象に `git describe --tags --always` を直接使っているが、**install.sh は `cd "$INSTALL_DIR/src"` した後に swiftc を実行している**。スクリプト copy 先の `$INSTALL_DIR/src` は **`.git` を持たない**ため、stamp 時の cwd 制御次第で `git describe` が「fatal: not a git repository」を返し fallback 経路（`unknown` または non-zero）に倒れる懸念がある。03 §2.3.2 で「`bash "$REPO_DIR/scripts/lib/version_stamp.sh" "$APP_DIR/Contents/Info.plist"` を追加」と書いているが、`$REPO_DIR` 配下を **cwd または `-C` で明示する**ことを実装内容に書く必要がある。 | P1 | 02 §3.1.2 / 03 §2.2.2 で「`git describe` は `-C "$REPO_DIR"` を付ける」または「`version_stamp.sh` の第2引数で REPO_DIR を受け取る」を明記。03 §2.3.2 も呼び出し側で REPO_DIR を渡す挙動に修正。 |
| P1-09 | 01 §2.2 UC1 シナリオ 1 / 00 §6 基準 1 | 「同 commit から 2 回 install しても CFBundleVersion が一致する」は、**作業ツリーが dirty な場合**（未コミット変更がある）に `git describe --tags --always --dirty` を使うかで挙動が変わる。02 §3.1.1 では `--dirty` の言及なし。受け入れ基準が「2 回連続実行」で「途中で作業ツリーを触らない」前提なら問題ないが、明示が無いと dirty マーカ付きでテスト緑にしづらい。 | P1 | 02 §3.1.1 / §10 に「`--dirty` は使わない（dirty 検出はビルドメタデータでスコープ外）」を明示。または「使う」場合は 01 UC1 / 00 §6 の受け入れ基準を「2 回連続実行（作業ツリー不変）で同値」と限定する。 |
| P1-10 | 01 §2.2 UC4 シナリオ 2 | 「Bundle.main.infoDictionary が空でも About がクラッシュしない」のテスト方針が、03 §2.5.3 で「純粋関数として `Sources/MacHealthKit/` に切り出して MacHealthCheck で網羅」と書かれているが、`Bundle.main.infoDictionary` 自体は副作用付き（実 Bundle 参照）のため、**純粋関数化される部分は `formatAboutVersionLine(_ shortVersion: String?)` のみ**で、`Bundle.main` 経路自体は単体テスト対象外になる。01 UC4 シナリオ 2 は受け入れ基準として手動確認に倒すか、`MacHealth.swift` 側の薄いガード（`as? String`）の手動レビューに倒すかを明示する必要がある。 | P1 | 01 UC4 シナリオ 2 の検証手段に「単体テストは `formatAboutVersionLine(nil)` 経路で代替し、`Bundle.main.infoDictionary` の nil 挙動はランタイム手動確認」を追記。02 §6.1 / 03 §2.5.3 にも同じ補足を入れる。 |
| P1-11 | 03 §2.8.2 docs 更新項目 | `docs/02_画面設計/README.md::G012` の `informativeText` 表記の更新方針が「動的取得（CFBundleShortVersionString 参照）」と書いてあるが、現行 docs（実装と照合）は「`バージョン 1.3`（`src/Info.plist::CFBundleShortVersionString` と同期）」と書かれている。動的取得後は **docs 側に具体 semver リテラルを書かない方向**にする旨を 03 §2.8.2 に明示する。 | P1 | 03 §2.8.2 に「informativeText の `バージョン X.Y` リテラルは `バージョン <CFBundleShortVersionString 動的取得値>` の表記に置換」を明示。 |
| P1-12 | 02 §10 ユーザー判断 / 03 §1.2 全体 | 02 §10-3「Info.plist テンプレ値」推奨 B 案 `0.0.0-DEV` だが、`plutil -lint` で plist 構造を検証する観点では `CFBundleVersion` は文字列（任意）なので問題ない一方、**macOS Launch Services は `CFBundleVersion` を文字列で比較し、`0.0.0-DEV` が `1.3` 等より新しいと誤判定される可能性**は残る。Launch Services 影響は 00 §3.3 で「`CFBundleShortVersionString` の意味・運用を変更しない」とあり、`CFBundleVersion` は本 issue の対象だが、テンプレ値配布が万一発生した場合の Launch Services 比較リスクは明示しておくべき。 | P1 | 02 §10-3 の選択肢に「Launch Services 比較順序の影響」のリスクを追記。00 §7.1 に「テンプレ値配布時の Launch Services 比較ロジック影響」を追記。 |
| P1-13 | 00 §1.2 背景 / 01 §2.2 UC4 リテラル | 既存実装 `src/MacHealth.swift::showAbout` のリテラル文言は「`バージョン 1.3`」**および**「`再起動なしで再起動相当の状態を保つ\n自動メンテナンスシステム\n\n• …`」を 1 つの multi-line 文字列リテラル `informativeText` 内に持つ。00/01 では `バージョン 1.3` リテラルのみ言及だが、`showAbout` 全体を `informativeText` ごと動的部分を組み立てる小関数で構成する旨は 03 §2.5 に揃える必要あり（リテラルの構造変更が大きすぎると差分が肥大化）。 | P1 | 03 §2.5.2 に「`informativeText` の最後の 1 行（`バージョン X.Y`）のみを置換する最小差分」と明示。`formatAboutVersionLine` の戻り値は `"バージョン X.Y"` 単独で、`informativeText` 組み立て側に concat する形にする旨を追加。 |
| P1-14 | 02 §6.1 表 / 03 §2.5.3 単体テスト | 02 §6.1 で `src/MacHealth.swift::showAbout` 動的取得の単体テスト対象を「`Sources/MacHealthKit/` 側にロジック切り出し」と書いているが、`MacHealthCheck` が現在テスト実装している経路（pure-core）と整合するため、**`Sources/MacHealthCheck/main.swift` に新規 useCase / scenario を追加する**ことを明記する必要がある。03 §2.5 では「`MacHealthCheck` に追加」と書いているが、02 §6.1 の表側に同じ記述を入れて整合性を取る。 | P1 | 02 §6.1 の該当行を「`Sources/MacHealthKit/Version.swift`（新規）に `formatAboutVersionLine` を実装、`Sources/MacHealthCheck/main.swift` に useCase 1 つ追加で 3 シナリオ網羅」と具体化。 |
| P1-15 | 03 §2.2.5 依存関係 | 「依存タスク: T1（テンプレ化済み Info.plist が必要）」と書いているが、`version_stamp.sh` 自体は引数で受けた任意の plist を stamp するため、**T1 完了前でも version_stamp.sh の実装と単体テストは可能**。順序的にも T1 と T2 を並列化できる旨を `1.2 実装順序` Mermaid 図と齟齬なく整理する。 | P2 | 03 §2.2.5 の依存を「依存タスクなし（T1 と並列実行可能）」に変更。`1.2` Mermaid の `t2 after t1` も並列に変更可能。 |
| P1-16 | 02 §2.1.1 表 / 03 §2.7.2 | `.agents/spec/04_仕様更新ルール.md` の更新箇所を、03 §2.7.2 では「`バージョン更新時の必須手順` の手順 2 を『動的取得化により不要』と明示」とあるが、**手順 2 を残して「自動追従するため省略可」と書く**のか「**手順 2 自体を削除する**」のか方針が曖昧。役割分離の正本ルール明記との整合上、削除でなく「手順 2 は About 動的取得化により**自動追従する**」と修正するのが妥当。 | P2 | 03 §2.7.2 に「手順 2 は削除せず、表現を『動的取得により自動追従するため手動更新不要』に書き換える」を明示。 |
| P2-17 | 全体 | 00/01/02/03 の用語表記で「stamp」「stamping」「打刻」「注入」が混在気味（02 §1.1 / 03 全体）。プロジェクト用語規約に倣い「stamp」に統一済みかどうかの確認。 | P2 | 用語一覧（01 §1.2）に「stamp」のみ載っており「注入」が二次表記として頻発するため、02 §10 で「stamp = 注入」を併記、または各所で揺れを最小化する程度（軽微）。 |
| P2-18 | 03 §1.2 Mermaid gantt | gantt 図で `T7 after t5` と書いているが、03 §2.7.5 では「依存タスク: T5（About 動的取得の実装と整合させる）」とあり整合 OK。一方 T6 が `after t4`、T8 が `after t7` であるが、T6 は実態として「既存 CI 流用確認」（実装ほぼなし）のため T1〜T5 と並列可能。 | P2 | gantt 図の `T6` 配置を `after t5` のままで OK だが、§4 テストの優先順位「T6 低」との整合は問題なし。修正不要レベル。 |
| P2-19 | 00 §1.2 / 01 §2.2 UC6 / 02 §1.1 / 03 §2.7 | `.agents/spec/04_仕様更新ルール.md` の「アプリバージョンの正本」見出しと「役割分離追記」の表記揺れ。本 issue で「正本は Info.plist」が崩れる懸念は無いことを 01 UC6 で確認しているが、04_仕様更新ルール の冒頭で「**`src/Info.plist` の `CFBundleVersion` / `CFBundleShortVersionString` とする**」と両方を正本扱いしている記述との二重整理が必要。本 issue 後は「`CFBundleVersion` は git 由来の自動 stamp 結果が正本（手動更新しない）」「`CFBundleShortVersionString` は手動 semver の正本」と二分割の明示が必要。 | P2 | 03 §2.7.2 に「04_仕様更新ルール §アプリバージョンの正本 の 1 行目を **役割分離した形（CFBundleVersion = git stamp、CFBundleShortVersionString = 手動 semver）に書き換える** ことを明示」を追加。 |
| P2-20 | 03 §2.6.2 | B 案採用時の semver タグ抽出 step が「`CFBundleShortVersionString` から tag 名を生成」とあるが、実装時に **`plutil -extract` / `PlistBuddy -c "Print :CFBundleShortVersionString"`** を使うか手段が不明。実装着手時に確定でよいが、02 §10-6 で「B 案は別 issue で B 案へ移行」と書いているため、03 §2.6.2 は A 案を主、B 案は補足扱いに整理する。 | P2 | 03 §2.6.2 の構成を「A 案: 変更なし（採用）」「B 案: 別 issue 化推奨（本 issue 範囲外）」に整理。 |

## 3. 本サイクルで実施する修正方針

cycle 1 では P0（P0-01〜P0-04）を最優先で修正し、続けて P1 を全件修正する。P2 は 0 件レビューを目指して同時に修正する（軽微な表記揺れ含む）。

## 4. 残件（次サイクル）

- cycle 2 で「全件修正 → 再レビュー → 指摘 0 件」を確認。
- workflow.db 書記委譲は最終 cycle 後に実施。

## 5. 実コードへの乖離（参考）

本依頼の範囲外（実コード修正は別 issue 推奨）の発覚事項:
- `make build` ターゲットは現状 `.app` を作らない（`build/MacHealth` バイナリのみ）。設計の「Makefile / install.sh の両方から stamp」は **実コードを変更しない限り Makefile からは stamp 不可**。ドキュメント側を「install.sh のみ stamp、`make build` は stamp 対象外」に整える。
- `create-release.yaml` の `runs-on: ubuntu-latest` は plutil 不可。本 issue では CI で stamp しない設計に倒すため実コード変更不要だが、ドキュメントの「macOS-latest を維持」表記は誤りなので訂正する。
