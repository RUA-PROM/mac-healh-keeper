# verify-and-close — 実装〜検証〜書記までの確認記録

- 取得プレフィックス: `20260529_114647`（`.agents/scripts/memo-prefix.sh` 実行値）
- 対象 issue: `.workflow/20260529_105524_ビルド時バージョン自動stamp/`
- command: verify-and-close
- 前提: review-docs cycle 2 で指摘 0 件確認済み。本 cycle は実装 + テスト + 04_review + 書記。

## 1. 実装（implement-feature 相当）

- T1: `src/Info.plist::CFBundleVersion` を `0.0.0-DEV` に置換、`CFBundleShortVersionString=1.3` を維持。`plutil -lint` OK。
- T2: `scripts/lib/version_stamp.sh` 新規（`set -euo pipefail` / `git -C "$REPO_DIR" describe --tags --always` / `plutil -replace CFBundleVersion -string`）。shellcheck（`-x --severity=warning`）OK。
- T3: `install.sh` の `.app` 組立直後に `version_stamp.sh` 呼び出しを追加（`$REPO_DIR` を第 2 引数で明示）。失敗時は警告ログ。`install.sh` の swiftc 行に `Version.swift` を追加。
- T5: `Sources/MacHealthKit/Version.swift` 新規（`formatAboutVersionLine(_ shortVersion: String?) -> String`、nil・空文字なら `"バージョン 不明"`）。`src/MacHealth.swift::showAbout` の末尾 1 行だけを動的化（他リテラル不変）。`Makefile build` の swiftc 行にも `Version.swift` を追加。
- T6: `.github/workflows/create-release.yaml` は A 案維持で**変更なし**。`docs/04_機能設計/CI・Release自動化/README.md` に「バージョン stamp の責務分離」節を追加し、CI で stamp しない理由（`ubuntu-latest` には `plutil` がない／`.app` を CI で組まないため）を明記。
- T7: `.agents/spec/04_仕様更新ルール.md` の「アプリバージョンの正本」「バージョン更新時の必須手順」を役割分離後の表現に書き換え。`docs/02_画面設計/README.md::G012` の `informativeText` を動的取得明示の表記に置換。`docs/01_システム概要/04_ディレクトリ構成/README.md` に `Version.swift` / `version_stamp.sh` / `version_stamp_test.sh` の行・ツリー図を追加、`src/Info.plist` の注記を `CFBundleShortVersionString=1.3` + `CFBundleVersion=0.0.0-DEV`（stamp 経路明記）に更新。`docs/04_機能設計/ローカル検証/README.md` の `make test` 内訳に `version_stamp_test.sh` を追記。
- T8: `Sources/MacHealthCheck/main.swift` に `formatAboutVersionLine` の useCase 1 件・scenario 3 件（"1.3" / nil / 空文字）を追加。`scripts/test/version_stamp_test.sh` を新規作成（10 ケース：git describe 一致 / 非 git fallback / 引数欠落 / 不正 plist / usage 出力）。`Makefile test-shell` のフォールバック配列に `version_stamp_test.sh` を追加（bats 経路は `bats scripts/test/` 一括実行のため追加不要）。
- 03_実装計画.md の各タスク見出し（T1/T2/T3/T5/T6/T7/T8）に `[完了]` マークを付与。

### 個別動作確認

- `scripts/lib/version_stamp.sh src/Info.plist $(git rev-parse --show-toplevel)` 経由で stamp 値 `v20260528.154638-1-gd313ef6` を確認、`git -C "$REPO_DIR" describe --tags --always` と完全一致。
- 非 git ディレクトリを `REPO_DIR` に渡すと fallback `0.0.0-DEV` を維持し終了コード 0。
- 引数 0 件 → 終了コード 2 + usage 出力。
- 存在しない plist → 終了コード 1。

## 2. `make check`

- 実行時刻: 2026-05-29 11:44 JST
- 結果: `==> all checks passed`
  - `lint-shell` / `lint-shfmt` / `lint-swift-format` / `lint-swiftlint` / `check-cycles` / `security-scan` / `test`: 全 OK
  - `test` 内訳:
    - `swift run MacHealthCheck`: 35 件 PASS / 0 件 FAIL（既存 32 件 + 本 issue 追加 3 件）
    - `swift test`: SKIP（XCTest 非搭載環境・既知の許容範囲）
    - shell tests: monitor 9 + metrics 17 + log_rotate 15 + install_metrics_smoke 8 + version_stamp 10 = **59 件 PASS / 0 件 FAIL**

## 3. 受け入れ基準（00 §6）の充足

| 基準 | 結果 |
| --- | --- |
| 1. 2 回 install で `CFBundleVersion` 一致 | OK（`git describe --tags --always` の決定性 + `version_stamp_test.sh` UC1-S1） |
| 2. `CFBundleShortVersionString` 手動 bump で About 自動追従 | OK（`formatAboutVersionLine` 経路 + Bundle 動的取得） |
| 3. tag 直上 commit で値一致 | OK（`version_stamp_test.sh` UC1-S1） |
| 4. tag 不在で `--always` 短縮 SHA | OK（経路網羅・空でないことを assert） |
| 5. `make check` 緑維持 | OK（94 件 PASS / 0 件 FAIL / SKIP 1 経路は許容） |
| 6. 04_仕様更新ルール 役割分離 | OK（同ファイル更新済） |

## 4. ユーザー判断ポイント（採用方針）

1. tag 命名規約: **A 案**（`vYYYYMMDD.HHMMSS` 日時タグ流用）
2. Info.plist テンプレ値: **B 案**（`0.0.0-DEV`）
3. PR #3 とのマージ順序: **同 PR に積み増し**（`feature/20260529` ブランチ継続）
4. About fallback 文言: **`"バージョン 不明"`**
5. CI fetch-depth: **A 案維持**（既存 `1` のまま）
6. stamp 入力: **`git describe --tags --always`**（`--dirty` なし、`--always` で fallback）

## 5. 04_review.md / 書記

- 04_review.md を新規作成（`document_id`: `C4C5618B-CC78-4BFA-BC5C-2A983048CEC4`）。
- 本 memo を `verify-and-close` 経由で workflow.db に記録予定（書記委譲）。
- `.workflow/close/` への移動はコミット + push 後に実施（追加 commit）。

## 6. 規約遵守

- `git add -A` / `git add .` を使わず、個別ファイルを対象に add する方針。
- `--no-verify` / `--amend` / `--force` 禁止を厳守。
- memo / 04_review の timestamp プレフィックスは `.agents/scripts/memo-prefix.sh` 経由でシステム時計から取得（手入力なし）。
- 既存 CI（`create-release.yaml`）は不変。リモート設定変更も実施せず。
