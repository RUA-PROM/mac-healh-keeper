---
document_id: "9F3B7A0D-2B6E-4F49-9C58-6F4B5C9DD7E4"
---

このドキュメントは、Mac Health Keeper のシステム仕様書（`docs/`）を v1.2.0 へ拡充した際の整合性レビュー結果を記録します（make check 導入・CI / Release 自動化・`scripts/lint/` の新規実装を docs に反映するためのレビュー）。

# システム仕様書レビュー結果（v1.2.0 拡充・make check / CI / Release）

**レビュー実施日時**: 2026 年 05 月 28 日 14:45:21 (JST)

**レビュー対象実装**:

- `.github/workflows/check.yml`（PR / `main` push 時の `make check` 実行）
- `.github/workflows/create-release.yaml`（`main` push 時の JST 日時タグ + `gh release --generate-notes`）
- `Makefile`（`check` / `lint` / `lint-shell` / `lint-shfmt` / `lint-swift-format` / `lint-swiftlint` / `check-cycles` / `security-scan` ターゲット）
- `scripts/lint/lib/common.sh`・`scripts/lint/run-shellcheck.sh`・`scripts/lint/run-shfmt.sh`・`scripts/lint/run-swift-format.sh`・`scripts/lint/run-swiftlint.sh`・`scripts/lint/check-source-cycles.sh`・`scripts/lint/security-scan.sh`
- `scripts/bin/monitor.sh`（`COOLDOWN_FILE` への `shellcheck disable=SC2034` 追加）
- `scripts/config/thresholds.sh`（ヘッダコメントと `shellcheck disable=SC2034` 追加）
- `scripts/test/metrics_test.sh`（`# shellcheck source=../lib/metrics.sh` 注釈 1 行追加）
- `README.md`（`make check` / CI / 任意ツールの brew install / Release 自動化の手順追記）

**レビュー対象 docs（更新後）**:

- `docs/README.md`（更新履歴に v1.2.0 追加）
- `docs/01_システム概要/01_プロジェクト概要/README.md`（§1.3 成果物・§1.4 技術スタックに開発フローを追加）
- `docs/01_システム概要/02_ステークホルダー/README.md`（§2.2 外部システムに GitHub Actions / Homebrew を追加）
- `docs/01_システム概要/03_アーキテクチャ/README.md`（§3.8.1 ローカル検証と CI / Release 自動化を新規節として追加）
- `docs/01_システム概要/04_ディレクトリ構成/README.md`（§4.1 / §4.3 / §4.4 / §4.7 / §4.8 に `scripts/lint/` と `.github/workflows/` を反映）
- `docs/04_機能設計/README.md`（F009 / F010 を機能一覧に追加）
- `docs/04_機能設計/ローカル検証/README.md`（F009・新規）
- `docs/04_機能設計/CI・Release自動化/README.md`（F010・新規）
- `docs/99_ID命名規則と管理/README.md`（F009 / F010・F009-S1/S2・F010-S1/S2 を追加）

**レビュー方針**:

- 実装ファイル（`.github/workflows/*.yml{,aml}`・`Makefile`・`scripts/lint/*`・`scripts/bin/monitor.sh`・`scripts/config/thresholds.sh`・`scripts/test/metrics_test.sh`・`README.md`）を直接読み、推測なく実装で確認できる事実のみを docs に反映。
- 既存レビュー成果物 `docs/00_review/20260528_112137_review.md` の構成を踏襲（カテゴリ別の指摘事項・確認済み項目・総合評価・修正実施結果）。
- DOCS_RULES（§Issue 完了時のシステム仕様書更新チェック）に従い、`docs/README.md` の更新履歴を更新し、`docs/00_review/` にレビュー結果を残す（issue を立てない）。
- レビューは **最低 3 ROUND かつ指摘ゼロまで反復**。指摘ゼロを確認した ROUND をもって完了とする。

---

## レビューの進行（ROUND 1 → ROUND 4）

各 ROUND ごとに観点を明示し、指摘 → 対応 → 末尾で残指摘の有無を再評価した。

### ROUND 1（観点: 機能存在の整合）

実装側に存在する 7 個の追加機能エリア（CI / Release 自動化 / Makefile check / scripts/lint 7 ファイル / monitor.sh の SC2034 / thresholds.sh の SC2034 / metrics_test.sh の source 注釈 / README.md の追記）に対し、docs 側で記載が抜けているものを洗い出した。

| # | 重大度 | 場所 | 内容 | evidence_source | 対応 |
| - | ------ | ---- | ---- | --------------- | ---- |
| R1-1 | critical | `docs/01_システム概要/04_ディレクトリ構成/README.md` §4.1 / §4.3 / §4.4 / §4.7 / §4.8 | `scripts/lint/` ディレクトリ（7 ファイル）と `.github/workflows/{check.yml, create-release.yaml}` の存在が記載されていない | existing_code（実物のファイル一覧と内容） | docs を実装に追従。トップツリー・役割表・詳細ツリー・ファイル別責務一覧（§4.8.6a / §4.8.9）を新設・追記。 |
| R1-2 | critical | `docs/01_システム概要/03_アーキテクチャ/README.md` §3.8 直後 | 開発フローに `make check` と CI / Release 自動化が組み込まれているが言及なし | existing_code（`Makefile::check` の実装）+ external_doc（GitHub Actions の挙動） | §3.8.1（ローカル検証と CI / Release 自動化）を新設。step 表・キー設定・タグ形式を実装と一致させて記述。 |
| R1-3 | critical | `docs/04_機能設計/README.md` 機能一覧 | F009 ローカル検証 / F010 CI・Release 自動化が未掲載 | existing_code | F009 / F010 を機能一覧に追加し、サブディレクトリ `ローカル検証/` `CI・Release自動化/` を新規作成（各 README）。 |
| R1-4 | critical | `docs/01_システム概要/01_プロジェクト概要/README.md` §1.3 成果物・§1.4 技術スタック | 開発フロー側の責務が未記載 | existing_code | §1.3 に「開発フロー」行、§1.4 に「検証ツール」「CI / Release」行を追加。 |
| R1-5 | warning | `docs/03_データ設計/README.md` §3.5 | scripts/lint の設定相当は新規には無いが、対象列挙ルールの記述場所が必要 | existing_code（`common.sh::list_shell_files` / `list_swift_files` の対象） | データ設計には設定値（閾値・ローテート設定）を置き、対象列挙ルールは F009 README に集約して二重管理を回避（spec/06: 単一責務）。 |
| R1-6 | warning | `docs/99_ID命名規則と管理/README.md` | F009 / F010 と関連 S（F009-S1/S2、F010-S1/S2）が未付与 | existing_code | 99.3（機能 ID）と 99.4（シーケンス図 ID）に追加。 |
| R1-7 | info | `docs/01_システム概要/02_ステークホルダー/README.md` §2.2 | GitHub Actions と Homebrew が外部システム一覧から漏れている | existing_code（`check.yml` 内 `brew install shellcheck`） | §2.2 に「GitHub Actions」「Homebrew」を追加。開発者の関心に `make check` / CI を追加。 |

**ROUND 1 末尾の残指摘**: 上記 R1-1〜R1-7 を全て対応済み。ROUND 1 残指摘ゼロ。

### ROUND 2（観点: 用語・命名整合・相互リンクの妥当性）

| # | 重大度 | 場所 | 内容 | evidence_source | 対応 |
| - | ------ | ---- | ---- | --------------- | ---- |
| R2-1 | info | F009/F010 README → 03 アーキテクチャ §3.8.1.2 / §3.8.1.3 の anchor | 日本語見出しは GitHub のアンカー生成が複雑（記号・Mermaid タグ等）で外部リンクが不安定 | inference_only（GitHub の Markdown アンカー仕様）+ existing_code（実セクション名） | アンカー指定をやめ、`§` 番号と doc 単位リンクのみに変更（`README.md#xxx` → `README.md`）。 |
| R2-2 | info | `docs/01_システム概要/04_ディレクトリ構成/README.md` §4.8.6a 末尾 | 自己リンク `(#489-githubworkflows)` の anchor が確実でない | inference_only（GitHub のアンカー仕様） | アンカー指定をやめ、`§4.8.9` のテキスト参照のみに変更。 |
| R2-3 | info | 用語の確認（`make check` の step 順序・`scripts/lint/lib/common.sh` の関数名） | docs と実装の用語ずれが無いか | existing_code（`Makefile::check` の for ループ・`common.sh` の関数定義） | `lint-shell lint-shfmt lint-swift-format lint-swiftlint check-cycles security-scan test` の順、`log_info/log_warn/log_skip/log_error`・`tool_available`・`repo_root`・`list_shell_files`・`list_swift_files` の関数名はすべて実装と一致。問題なし。 |

**ROUND 2 末尾の残指摘**: R2-1 / R2-2 を対応済み。R2-3 は確認済み（指摘なし）。ROUND 2 残指摘ゼロ。

### ROUND 3（観点: 図表・データフロー整合・更新履歴・機械的検証）

| # | 重大度 | 場所 | 内容 | evidence_source | 対応 |
| - | ------ | ---- | ---- | --------------- | ---- |
| R3-1 | info | F009-S1 の flowchart の step 順 | `Makefile::check` の for ループの step 順と一致しているか | existing_code（`Makefile`） | 完全一致を確認（`lint-shell, lint-shfmt, lint-swift-format, lint-swiftlint, check-cycles, security-scan, test`）。問題なし。 |
| R3-2 | info | F010-S1 / F010-S2 の sequenceDiagram | キー設定（`permissions` / `concurrency` / `runs-on` / `timeout-minutes`）が実装と一致しているか | existing_code（`.github/workflows/*.yml{,aml}`） | check.yml: `permissions.contents: read`・`concurrency.cancel-in-progress: true`・`runs-on: macos-latest`・`timeout-minutes: 30`、create-release.yaml: `permissions.contents: write`・`concurrency.cancel-in-progress: false`・`runs-on: ubuntu-latest`、いずれも一致。問題なし。 |
| R3-3 | info | `docs/README.md` 更新履歴 | v1.2.0 の追記が必要 | existing_code（DOCS_RULES.md §更新履歴） | v1.2.0 行を追加。 |
| R3-4 | info | 機械的検証（document_id 一意性 / `/Users/` 検出 / リンク切れ） | docs 全体の健全性 | test_output（`grep`/`python3 os.path.isfile`） | document_id 重複 0 件、`/Users/` 0 件（旧レビュー内の引用のみ）、リンク切れ 0 件。問題なし。 |

**ROUND 3 末尾の残指摘**: R3-3 を対応済み。R3-1 / R3-2 / R3-4 は確認のみで指摘なし。ROUND 3 残指摘ゼロ。

### ROUND 4（観点: 実装側軽微の必要性精査・横断的最終確認）

| # | 重大度 | 場所 | 内容 | evidence_source | 対応 |
| - | ------ | ---- | ---- | --------------- | ---- |
| R4-1 | info | `scripts/lint/` の全 7 ファイル | docs での参照網羅性（少なくとも 1 docs で説明されているか） | test_output（`grep -rl` で各ファイル名検索） | 7 ファイルすべて 3 docs 以上で参照されている（04_機能設計/ローカル検証・04_ディレクトリ構成・04_機能設計/CI・Release自動化）。問題なし。 |
| R4-2 | info | 実装側軽微修正の要否 | docs が正で実装が誤りという致命的乖離は無いか | existing_code（実装読了） | `make check` / scripts/lint / CI / Release / monitor.sh / thresholds.sh / metrics_test.sh の修正分はいずれも docs と整合しており、実装側に追加修正は不要。 |
| R4-3 | info | DOCS_RULES.md §Issue 完了時のチェック | docs/README.md 更新履歴・00_review に成果物がある・新規 issue を立てていない | existing_code（DOCS_RULES.md） | 全てを満たしている。問題なし。 |

**ROUND 4 末尾の残指摘**: なし。**指摘ゼロ**。

---

## 1. システム概要（01_システム概要）のレビュー

### 1.1. 指摘事項

なし（ROUND 1〜4 で全件対応済み）。

### 1.2. 確認済み項目

#### 確認済み項目 1: アーキテクチャ §3.8.1 の新設

**確認結果**: ✅ 整合性あり

**確認内容**:

- `docs/01_システム概要/03_アーキテクチャ/README.md` §3.8.1（ローカル検証と CI / Release 自動化）を新設。
- §3.8.1.1: `make check` の 7 step 表（`lint-shell` / `lint-shfmt` / `lint-swift-format` / `lint-swiftlint` / `check-cycles` / `security-scan` / `test`）を `Makefile::check` の for ループ順に記載。
- §3.8.1.2: `.github/workflows/check.yml` のキー設定（`runs-on: macos-latest` / `timeout-minutes: 30` / `permissions.contents: read` / `concurrency.cancel-in-progress: true`）を実装どおり記載。
- §3.8.1.3: `.github/workflows/create-release.yaml` のキー設定（`runs-on: ubuntu-latest` / `permissions.contents: write` / `concurrency.cancel-in-progress: false`）と JST 日時タグ生成式（`TZ=Asia/Tokyo date '+%Y%m%d.%H%M%S'`）を実装どおり記載。

#### 確認済み項目 2: ディレクトリ構成 §4.1 / §4.3 / §4.4 / §4.7 / §4.8

**確認結果**: ✅ 整合性あり

**確認内容**:

- §4.1 トップレベル構成のツリーと役割表に `scripts/lint/`・`.github/workflows/` を追加。
- §4.3 シェル系の表に `scripts/lint/lib/common.sh`・`run-shellcheck.sh`・`run-shfmt.sh`・`run-swift-format.sh`・`run-swiftlint.sh`・`check-source-cycles.sh`・`security-scan.sh` の 7 行を追加。
- §4.4 配置物・運用に `.github/workflows/{check.yml, create-release.yaml}` を追加し、`Makefile` 行に `make check` 集約と個別ターゲットを記載。
- §4.7 詳細ツリーに `scripts/lint/`・`.github/workflows/` を追加。
- §4.8.6a として scripts/lint/ のファイル別責務一覧を新設、§4.8.9 として `.github/workflows/` の表を新設。

#### 確認済み項目 3: プロジェクト概要・ステークホルダー

**確認結果**: ✅ 整合性あり

**確認内容**:

- 01_プロジェクト概要 §1.3 に「開発フロー」行を追加、§1.4 に「検証ツール」「CI / Release」の 2 行を追加。
- 02_ステークホルダー §2.2 に「GitHub Actions」「Homebrew」の 2 行を追加。開発者の関心欄に `make check` / CI / Release のリンクを追加。

---

## 2. 画面設計（02_画面設計）のレビュー

### 2.1. 指摘事項

なし（変更なし。今回追加の実装は UI に影響しないため）。

### 2.2. 確認済み項目

UI 関連の実装変更は無いため、`02_画面設計/README.md` は v1.1.0 のまま整合性を維持。

---

## 3. データ設計（03_データ設計）のレビュー

### 3.1. 指摘事項

なし（変更なし）。

### 3.2. 確認済み項目

#### 確認済み項目 1: 設定値の二重管理回避

**確認結果**: ✅ 整合性あり

**確認内容**: `scripts/lint/` 配下には「閾値」「設定変数」は存在せず、`common.sh::list_shell_files` / `list_swift_files` の対象列挙は「振る舞いの規定」に近い。これは 03_データ設計（永続データ・設定値）の責務ではないため、04_機能設計 / ローカル検証に集約し二重管理を回避（spec/06 単一責務）。

---

## 4. 機能設計（04_機能設計）のレビュー

### 4.1. 指摘事項

なし。F009 / F010 を新設して網羅。

### 4.2. 確認済み項目

#### 確認済み項目 1: F009 ローカル検証（新規）

**確認結果**: ✅ 整合性あり

**確認内容**:

- F009-S1: `make check` の処理フロー（Mermaid flowchart）。`Makefile::check` の for ループの 7 step（`lint-shell` `lint-shfmt` `lint-swift-format` `lint-swiftlint` `check-cycles` `security-scan` `test`）と rc 集約挙動を実装どおり記述。
- F009-S2: 各ランナーの I/F（必須／任意・終了コード・主要オプション）を実装どおり記述。`shellcheck -x --severity=warning`・`shfmt -d -i 4 -ci`・`swift-format lint --strict`・`swiftlint --strict`。
- `list_shell_files` の対象範囲（`scripts/{bin,lib,config,test}/*.sh` + `scripts/bin/mac-health` 拡張子なし + `install.sh` / `uninstall.sh`）・`list_swift_files` の対象範囲（`Sources/**/*.swift` + `Tests/**/*.swift`）を実装どおり記述。
- 6 種のセキュリティパターンを `security-scan.sh` の `$patterns` ヒアドキュメントから抜粋して記述。`# noqa: security` / `// noqa: security` の除外マーカ、自己除外（`security-scan.sh`・`check-source-cycles.sh`・`lib/common.sh`）も記載。
- `check-source-cycles.sh` の挙動（`^[[:space:]]*(source\|\.) ` 抽出 → `$ROOT_DIR=scripts` / `$SCRIPT_DIR=<file dir>` 置換 → 動的依存スキップ → 正規化 → awk DFS の color[]）を 7 step で記述。

#### 確認済み項目 2: F010 CI・Release 自動化（新規）

**確認結果**: ✅ 整合性あり

**確認内容**:

- F010-S1: CI（`check.yml`）の処理フロー（sequenceDiagram）。`actions/checkout@v4` → `command -v shellcheck` 判定 → 不在時 `brew install shellcheck` → `make check` の流れを実装どおり記述。
- F010-S2: Release 自動化（`create-release.yaml`）の処理フロー（sequenceDiagram）。`TZ=Asia/Tokyo date '+%Y%m%d.%H%M%S'` → `git tag` → `git push origin "$TAG"` → `gh release create "$TAG" --generate-notes` を実装どおり記述。
- キー設定（`permissions` / `concurrency` / `runs-on` / `timeout-minutes` / `env.GH_TOKEN`）は両ワークフローとも実装と一致。
- 既知の制約（shellcheck 同梱状況・タグ衝突・フォーク PR・macos 依存・任意ツールの SKIP）を運用観点でメモ。

---

## 5. エラー処理と外部通知（05_エラー処理と外部通知）のレビュー

### 5.1. 指摘事項

なし（変更なし）。

### 5.2. 確認済み項目

`scripts/lint/` 配下のランナーは読み取り専用ユーティリティで、本プロジェクトのジョブ実行系（monitor / docker / uptime / refresh）のエラー処理パスを通らないため、05_エラー処理と外部通知への影響なし（既存の方針は v1.1.0 のまま維持）。

---

## 6. ID 命名規則と管理（99_ID命名規則と管理）のレビュー

### 6.1. 指摘事項

なし。F009 / F010 と関連 S（F009-S1/S2、F010-S1/S2）を 99.3 / 99.4 に追加。

### 6.2. 確認済み項目

#### 確認済み項目 1: F009 / F010 の機能 ID

**確認結果**: ✅ 整合性あり

**確認内容**: 99.3 の表に F009「ローカル検証」と F010「CI・Release 自動化」を追加。各行が対応する `04_機能設計/<機能名>/README.md` へリンク。

#### 確認済み項目 2: F009-S1 / F009-S2 / F010-S1 / F010-S2 のシーケンス図 ID

**確認結果**: ✅ 整合性あり

**確認内容**: 99.4 の表に 4 行を追加。F009-S1（`make check` 処理フロー・flowchart）、F009-S2（各ランナーの I/F・表）、F010-S1（CI 処理フロー・sequenceDiagram）、F010-S2（Release 自動化処理フロー・sequenceDiagram）。

---

## 7. 機械的検証結果

### 7.1. document_id 一意性

```
$ grep -rn '^document_id:' docs/ | awk -F'"' '{print $2}' | sort | uniq -d
(空)
```

新規 2 ドキュメント（`4155181A-...`・`8BC9A5F2-...`）を含めても重複なし。

### 7.2. 機密情報の混入

```
$ grep -rn '/Users/' docs/
docs/00_review/20260528_112137_review.md（前回レビュー内の引用のみ・5 行）
```

新規追記分には `/Users/` の絶対パスなし。`$HOME` / `~` 表記を維持。

### 7.3. リンク切れチェック

```
$ python3 -c '... os.path.isfile ...'
total broken: 0
```

新規 2 docs を含む docs 全体でリンク切れ 0 件。

### 7.4. 新キーワードの参照網羅性

```
$ grep -rn 'create-release|check.yml|make check|scripts/lint|F009|F010' docs/ | wc -l
107
```

`make check` / CI / Release / scripts/lint / F009 / F010 が docs 全体で 107 箇所参照されている（新キーワードが docs 全体に行き渡っている証跡）。

### 7.5. scripts/lint 全 7 ファイルの docs での参照網羅性

```
security-scan.sh:        3 files
run-shfmt.sh:            3 files
run-swiftlint.sh:        3 files
run-shellcheck.sh:       3 files
run-swift-format.sh:     3 files
check-source-cycles.sh:  3 files
lib/common.sh:           3 files
```

7 ファイルそれぞれが少なくとも 3 docs で参照されている（04_機能設計/ローカル検証・04_ディレクトリ構成・04_機能設計/CI・Release自動化）。

### 7.6. Makefile::check の step 順序と F009-S1 図の一致

| Makefile（実装） | F009-S1（docs flowchart） |
| ---------------- | ------------------------ |
| `lint-shell` | `lint-shell` |
| `lint-shfmt` | `lint-shfmt` |
| `lint-swift-format` | `lint-swift-format` |
| `lint-swiftlint` | `lint-swiftlint` |
| `check-cycles` | `check-cycles` |
| `security-scan` | `security-scan` |
| `test` | `test` |

完全一致。

---

## 8. 総合評価

### 8.1. レビュー結果サマリー

| カテゴリー | 指摘事項数 | 確認済み項目数 | 評価 |
| ---------- | ---------- | -------------- | ---- |
| システム概要 | 0（ROUND 1 で 4 件→対応済み） | 3 | ✅ |
| 画面設計 | 0 | 1（影響なしの確認） | ✅ |
| データ設計 | 0 | 1 | ✅ |
| 機能設計 | 0（ROUND 1 で 1 件→F009/F010 新設で対応済み） | 2 | ✅ |
| エラー処理と外部通知 | 0 | 1（影響なしの確認） | ✅ |
| ID 命名規則と管理 | 0（ROUND 1 で 1 件→対応済み） | 2 | ✅ |

### 8.2. 総合評価

✅ **合格**。`make check` 導入と `.github/workflows/{check.yml, create-release.yaml}` および `scripts/lint/` 配下 7 ファイルの実装を、docs 側に **F009 ローカル検証**・**F010 CI・Release 自動化** の 2 機能として位置づけ直し、関連する 8 docs を同時更新した。`Makefile::check` の step 順序、`.github/workflows/*.yml{,aml}` のキー設定、`scripts/lint/*` の各ランナーのオプション・対象列挙ルール、6 種のセキュリティパターンを実装どおり記述した。

レビューは **4 ROUND** 実施し、各 ROUND 末尾で残指摘を再評価。ROUND 4 末尾で **指摘ゼロ** を確認した。

### 8.3. 修正実施結果

#### 8.3.1. 修正・追加した docs/ ファイル一覧

| ファイル | 種別 | 差分要約 |
| -------- | ---- | -------- |
| `docs/README.md` | 更新 | 構成セクションに「ローカル検証 / CI・Release 自動化」を追加。更新履歴に v1.2.0 行を追加。 |
| `docs/01_システム概要/01_プロジェクト概要/README.md` | 更新 | §1.3 に「開発フロー」行、§1.4 に「検証ツール」「CI / Release」行を追加。 |
| `docs/01_システム概要/02_ステークホルダー/README.md` | 更新 | §2.2 に「GitHub Actions」「Homebrew」行を追加。開発者の関心に `make check` / CI のリンクを追加。 |
| `docs/01_システム概要/03_アーキテクチャ/README.md` | 更新 | §3.8.1（ローカル検証と CI / Release 自動化）を新設（§3.8.1.1 / §3.8.1.2 / §3.8.1.3）。 |
| `docs/01_システム概要/04_ディレクトリ構成/README.md` | 更新 | §4.1 / §4.3 / §4.4 / §4.7 / §4.8 に `scripts/lint/` と `.github/workflows/` を反映（§4.8.6a / §4.8.9 を新設）。 |
| `docs/04_機能設計/README.md` | 更新 | 機能一覧に F009 / F010 を追加。 |
| `docs/04_機能設計/ローカル検証/README.md` | **新規** | F009 機能 README（document_id: `4155181A-5B86-45D2-A28D-00B431F1F31C`）。F009-S1（処理フロー）・F009-S2（各ランナー I/F）・6 種パターン・対象列挙ルール・既知の制約を網羅。 |
| `docs/04_機能設計/CI・Release自動化/README.md` | **新規** | F010 機能 README（document_id: `8BC9A5F2-07BD-4597-BA28-ACF4ACECF1E4`）。F010-S1（CI 処理フロー）・F010-S2（Release 自動化処理フロー）・キー設定・JST タグ形式・既知の制約を網羅。 |
| `docs/99_ID命名規則と管理/README.md` | 更新 | 99.3 に F009 / F010、99.4 に F009-S1/S2・F010-S1/S2 を追加。 |
| `docs/00_review/20260528_144521_review.md` | **新規（本ファイル）** | v1.2.0 拡充の集約レビュー結果。ROUND 1〜4 を記録。 |

#### 8.3.2. 修正・追加した実装側ファイル

なし（実装側に追加修正は不要と判断。docs を実態に追従させる第一義の方針で完了）。

### 8.4. 別 issue 化の提案

特になし。`docs/00_review/` への集約のみで完結（DOCS_RULES §基本方針：issue を立てない）。

### 8.5. 次のステップ

- 実装変更時に各 docs を更新し、必要に応じて `docs/00_review/` に新しい `YYYYMMDD_HHMMSS_review.md` を追加する。
- 新規ランナーを `scripts/lint/` に追加する場合は `Makefile::check` の step 列、04_機能設計 / ローカル検証 §F009-S1 の flowchart と §F009-S2 の I/F 表、04_ディレクトリ構成 §4.3 / §4.7 / §4.8.6a を同時更新する。
- CI / Release ワークフローのキー設定（`permissions` / `concurrency`）を変更した場合は 04_機能設計 / CI・Release 自動化 §F010 のキー設定表を同時更新する。
- 任意ツール（`shfmt` / `swift-format` / `swiftlint`）を CI で必須化したくなったら `check.yml` の `Ensure shellcheck` step に `brew install` を追加し、04_機能設計 / CI・Release 自動化 §既知の制約を更新する。

---

## 9. 確認方法（最終 ROUND の「残指摘ゼロ」確認）

ROUND 4 末尾で次を確認した：

1. **ROUND 1 で挙げた R1-1〜R1-7 が docs に反映されている**（grep / 目視）。
2. **ROUND 2 で挙げた R2-1〜R2-2 のアンカー指定を解消した**（Edit 結果）。
3. **ROUND 3 で挙げた R3-3（更新履歴）を反映した**（Edit 結果）。
4. **ROUND 4 で実装 7 ファイル全てが少なくとも 3 docs で参照されている**（grep）。
5. **document_id 重複ゼロ・`/Users/` 機密混入ゼロ・リンク切れゼロ**（機械的検証）。
6. **`Makefile::check` の step 順序と F009-S1 図の step 順序が完全一致**（実装読了 + 目視）。
7. **実装側に追加修正不要**（docs が正で実装が誤りの致命的乖離なし）。

→ **指摘ゼロ**。

---

## 参考資料

- [`.agents/DOCS_RULES.md`](../../.agents/DOCS_RULES.md)
- [docs/README.md](../README.md)
- [01 システム概要 / 03 アーキテクチャ §3.8.1](../01_システム概要/03_アーキテクチャ/README.md)
- [01 システム概要 / 04 ディレクトリ構成 §4.8.6a / §4.8.9](../01_システム概要/04_ディレクトリ構成/README.md)
- [04 機能設計 / ローカル検証](../04_機能設計/ローカル検証/README.md)
- [04 機能設計 / CI・Release 自動化](../04_機能設計/CI・Release自動化/README.md)
- [99 ID 命名規則と管理](../99_ID命名規則と管理/README.md)
- 前回レビュー: [`docs/00_review/20260528_112137_review.md`](./20260528_112137_review.md)

---

**最終更新**: 2026 年 05 月 28 日 14:45:21 (JST) / **maintainer**: docs worker
